import React from "react";
import LoginForm from "@/components/auth/login-form";

// Login route rendering within AuthModal
export default function LoginPage(): React.JSX.Element {
  return <LoginForm />;
}
