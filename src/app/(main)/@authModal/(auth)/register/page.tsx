import React from "react";
import RegisterForm from "@/components/auth/register-form";

// Register route rendering within AuthModal
export default function RegisterPage(): React.JSX.Element {
  return <RegisterForm />;
}
