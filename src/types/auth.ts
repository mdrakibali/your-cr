import React from "react";

// Props for intercepted auth modal container
export interface AuthModalProps {
  children: React.ReactNode;
}

// Props for reset password form component
export interface ResetPasswordFormProps {
  token?: string;
}

